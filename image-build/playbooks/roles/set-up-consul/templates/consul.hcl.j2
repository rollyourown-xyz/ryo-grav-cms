# SPDX-FileCopyrightText: 2022 Wilfred Nicoll <xyzroller@rollyourown.xyz>
# SPDX-License-Identifier: GPL-3.0-or-later

datacenter  = "{{ host_id }}"
data_dir    = "/opt/consul"
domain      = "ryo"

server      = false

#
# THE FOLLOWING IS EXPERIMENTAL

#client_addr = "0.0.0.0"
client_addr = "[::]"

#bind_addr             = "0.0.0.0"
bind_addr             = "[::]"

#retry_join = [ "{{ lxd_host_network_part }}.1" ]
retry_join = [ "{{ host_public_ipv6_prefix }}::{{lxd_host_network_ipv6_subnet}}:1" ]

# END EXPERIMENTAL
#

retry_interval = "10s"
